#include <iostream>

using namespace std;

/*
  Heap is some sorting thing
*/

int main() {
  int max;
  int min;
  int next;
  int right;
  int left;
  char input[30];
  int* a = new int[10];
  int* b = new int[10];
  int* c = new int[10];

  while (true) {
  cin >> input;
  
  for (int i = 0; i < 5; i++) {
    int tempo;
    (int)input[i];
    
    //cout << "a" << endl;
    a[i] = (int)input[i];
    //cout << "b" << endl;
    b[i] = (int)input[i];
    //cout << "c" << endl;
    c[i] = (int)input[i];

    a[i] = max;
    
    //cout << "[" << a << "] " << "[" << b << "] " << "[" << c << "]";
    return true;
    }

  //If the number is big, move it to the right
     if (input.max > input.min) {
       int temp;
       
    }
  }
}

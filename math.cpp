#include <iostream>
#incude "tree.cpp"

using namespace std;
//alt + x + linum-mode

int main() {
  Tree* t = new Tree();
  char input[100];

  while(true) {
    cout << endl << "Enter a math problem.";
    cin >> input;

    //Checks if only numbers and operators
    if () {
      cout << "Only numbers and operators."
	return 1;
    }
  }
}
